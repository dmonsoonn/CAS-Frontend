services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cas-frontend
    ports:
      - '3000:80'
    restart: on-failure
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
